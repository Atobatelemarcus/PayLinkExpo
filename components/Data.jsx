/*login bloc
 const storedUser = JSON.parse(localStorage.getItem("user"));

    if (!storedUser) {
      alert("No user found. Please register first.");
      return false;
    }

    // Check email or phone + password
    if ((storedUser.email === identifier || storedUser.phone === identifier) && storedUser.password === password) {
      setUser(storedUser);
      navigate("/DashboardHome");
      return true;
    } else {
      alert("Incorrect email/phone or password");
      return false;
    }*/